{
  "name": "llm-product-profiler-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for LLM Product Profiler - handles CORS proxy and Azure OpenAI API",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production",
    "tail": "wrangler tail",
    "test": "wrangler dev --test"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "azure-openai",
    "cors-proxy",
    "llm"
  ],
  "author": "Adobe Garage Week",
  "license": "PROPRIETARY",
  "devDependencies": {
    "wrangler": "^3.22.0"
  }
}

